package Inventory;

import java.awt.*;
import java.sql.*;
import java.util.Date;

import javax.swing.*;

import ItemCheckout.*;
public class Garbage {
 
  public static void main(String args[]) {
    JFrame frame = new JFrame("Test");
    frame.setVisible(true);
    frame.setSize(600,600);
    frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    
    JPanel panel = new JPanel();
    
    
    String[] options = {"in", "out", "missing", "overdue", "delete"};
    JComboBox<?> state = new JComboBox<Object>();
    frame.add(state);
    frame.add(panel);

    JLabel itemId = new JLabel("tbd");
    panel.add(itemId);
  }
}


